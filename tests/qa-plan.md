# 《职场营销博弈》QA 计划与质量门判定

> 文档负责人：严守真（Quality Lead）
> 版本：v0.1.0-draft
> 日期：2026-08-17
> 标注约定：【已验证】= 已有证据支撑；【建议】= 待执行/待决策；【计划】= 尚未实施，仅为规划。

---

## 1. 项目概览与已验证事实

### 1.1 技术栈与形态
- 纯 Python 3.13 标准库桌面游戏，tkinter GUI。
- 单文件 `.exe` 交付（PyInstaller 构建）：离线、本地内嵌启发式 AI（非 LLM）。
- 核心数据：情况牌 256 张、12 类、每张 4 选项（共 1024 选项），内嵌于 `src/cards_data.py`。

### 1.2 核心循环（玩法边界）
- 12 天 × 6 阶段：晨会 / 情况牌 / 午间闲谈 / 夜间行动 / 日结 / 联席会议。
- 联席会议日 3/6/9/12 进行白盒投票；玩家可花预算「指认」或「自保」。
- 失败条件：玩家压力 ≥ 100，或被投票出局 → 「离职观察者」结局（非黑屏，属设计内结局）。
- 胜利条件：撑到 D12 且（达成派系目标 或 声望 ≥ 80）→ 胜；否则 → 负。

### 1.3 已验证事实（来自开发/构建记录）
| 编号 | 验证项 | 命令 / 场景 | 结果 |
|------|--------|-------------|------|
| (a) | 开发态随机策略稳定性 | `python main.py --headless 30 --difficulty normal --seed 7` | 崩溃 0，结局 {win:15, observer:15, lose:0} |
| (b) | 冻结态基础运行 | `dist/职场营销博弈.exe --headless 10`（独立临时目录，无项目文件） | 退出码 0，崩溃 0，无 `crash.log` |
| (c) | 冻结态内嵌数据兜底 | 去除 `--add-data`、强制走内嵌副本：`--headless 6 --difficulty hard --seed 11` | 崩溃 0，退出码 0 |

> **结论（已验证）**：构建产物可在无项目文件的隔离环境中稳定启动并完成 headless 模拟；内嵌数据兜底已修复此前 Windows `_MEIPASS` 偶发 `PermissionError` 崩溃。

### 1.4 已知问题（必须写入并作为 CONCERNS）
- **平衡偏差**：在随机策略（headless）下 `lose` 恒为 0 —— 玩家几乎不会「输」，说明难度与负反馈不足。属平衡问题，需 design/数值调参（见 `config/tuning.json` 与 `design/gdd/balance.md`）。**严重级：High。**

---

## 2. 测试策略

### 2.1 单元测试【计划·暂缺】
> 当前项目以单文件构建 + headless 集成验证为主，模块级单元测试尚未建立。

| 模块（建议） | 建议测试点 | 状态 |
|--------------|-----------|------|
| `cards_data.py` 数据完整性 | 256 张 × 4 选项齐备、12 类分布、无空/重复文本 | 【计划】 |
| 压力/声望结算 | 压力 ≥ 100 触发离职观察者；声望 ≥ 80 判定胜利 | 【计划】 |
| 联席会议投票 | 指认/自保预算扣减、出局判定、白盒计票 | 【计划】 |
| 存档读写 | 存档序列化/反序列化一致性、版本兼容 | 【计划】 |
| 内嵌资源兜底 | 外部数据缺失时回退内嵌副本 | 【计划】 |

**落地建议**：在 v0.2.0 前引入 `pytest`，对纯逻辑层（结算/投票/数据完整性）补齐单测，把 headless 模拟从「集成级」下沉为「单元级」信号。

### 2.2 冒烟测试【已验证命令 + 通过标准】
> 冒烟范围 = 核心循环可在隔离环境下零崩溃启动并收敛。详见第 3 节命令与标准。

### 2.3 回归测试【建议】
- 回归目标：每次合并后重跑 (a)(b)(c) 三套命令，确保零崩溃与退出码 0 不回退。
- 回归触发：构建产物变更、`cards_data.py` / 结算逻辑 / 投票逻辑改动。
- 建议维护 `tests/regression-suite.md` 记录历史通过基线（含 seed 与 difficulty）。
- 已知回归缺口：平衡偏差（lose=0）**不能**作为回归通过的「期望」，需随调参引入新的期望分布。

### 2.4 手动 GUI 验收清单【建议·需人工签收】
> 以下为 GUI 路径手动验收项，headless 无法覆盖渲染与交互。每项需人工标记 PASS/FAIL 并留存截图/录屏。

| 编号 | 验收项 | 检查点 | 签收 |
|------|--------|--------|------|
| G1 | 启动 | 双击 exe 进入主菜单，无报错、无 `_MEIPASS` 权限弹窗 | ☐ |
| G2 | 新游戏 | 选择难度/派系后进入 D1 晨会，状态初始化正确 | ☐ |
| G3 | 出牌（情况牌） | 256 张牌随机出现、4 选项可点击、选择后结算生效 | ☐ |
| G4 | 夜间行动 | 预算扣减、行动选择持久化到日结 | ☐ |
| G5 | 日结 | 压力/声望/资源数值正确刷新，与公式一致 | ☐ |
| G6 | 联席会议（D3/6/9/12） | 指认/自保交互、投票计票、出局/留任动画正确 | ☐ |
| G7 | 存档读档 | 退出后再进可读取进度，数值与离开前一致 | ☐ |
| G8 | 结局画面 | 胜 / 负 / 离职观察者 三种画面均正常渲染（非黑屏） | ☐ |

---

## 3. 冒烟测试命令与通过标准

### 3.1 开发态【已验证】
```bash
python main.py --headless 30 --difficulty normal --seed 7
```
- 通过标准：崩溃数 = 0；退出码 = 0；无 `crash.log`。
- 附加观察（非门控）：结局分布应随调参出现非零 `lose`。

### 3.2 冻结态（标准构建）【已验证】
```bash
dist/职场营销博弈.exe --headless 10
```
- 环境：独立临时目录，无项目文件（验证可移植性）。
- 通过标准：退出码 = 0；崩溃数 = 0；无 `crash.log`。

### 3.3 冻结态（内嵌兜底强制）【已验证】
```bash
# 去除 --add-data 的构建，强制走内嵌副本
dist/职场营销博弈.exe --headless 6 --difficulty hard --seed 11
```
- 通过标准：崩溃数 = 0；退出码 = 0。
- 目的：验证此前 `_MEIPASS` 权限崩溃已修复。

> **通用通过标准（冒烟门）**：崩溃数 = 0 且 退出码 = 0 且 未生成 `crash.log`。三项全满足方为 PASS。

---

## 4. Bug 严重级分级标准

| 级别 | 定义 | 响应 | 示例 |
|------|------|------|------|
| **Blocker** | 导致程序无法启动/数据丢失/构建不可用，无变通方案 | 立即停发，阻断发布 | exe 在干净环境双击直接崩溃；存档文件损坏不可读 |
| **High** | 核心玩法可被绕过或关键平衡/体验严重失真，有或部分变通 | 发布前必须修复或显式豁免 | `lose=0` 玩家永不失败（本次）；联席会议投票计票错误 |
| **Medium** | 非核心功能缺陷，有明确变通方案 | 排期修复，可不阻断发布 | 某张情况牌文本错位；夜间行动描述文案错误 |
| **Low** | 外观/文案/边缘体验瑕疵 |  backlog 累积 | 按钮 hover 色差；个别标点全半角不一致 |

---

## 5. 已知风险与缓解

### 5.1 「lose=0」平衡偏差 【已验证现象·严重级 High】
- **现象**：随机策略 headless 30 局，结局 `{win:15, observer:15, lose:0}`，玩家永不出现「负」结局。
- **影响**：难度与负反馈缺失，胜利几乎无代价，长期留存与博弈张力受损。
- **根因假设（建议复核）**：胜负阈值偏宽、AI 攻击性不足、负面事件权重过低、玩家初始资源偏充裕。
- **缓解（当前）**：已在文档标注为 CONCERNS，纳入平衡质量门，要求 design/数值在后续 sprint 调参（依据 `config/tuning.json`、`design/gdd/balance.md`）。
- **是否阻断发布**：建议作为「发布豁免项」由用户/设计显式确认（见第 6 节总体放行建议）。

### 5.2 冻结态数据加载权限风险 【已验证已缓解】
- **历史问题**：Windows 下 PyInstaller `_MEIPASS` 偶发 `PermissionError`，导致数据加载崩溃。
- **缓解（已验证）**：构建去除 `--add-data`、强制走内嵌副本；验证 (c) 在 hard/seed11 下崩溃 0、退出码 0。
- **残余风险**：极端环境（只读介质/受限临时目录）仍建议监控 `crash.log`；保留内嵌副本为唯一真源。

---

## 6. 质量门判定

| 质量门 | 结论 | 依据 |
|--------|------|------|
| 构建质量门 | **PASS** | 单文件 exe 构建成功；冻结态 headless 0 崩溃（(b)(c) 已验证） |
| 功能质量门 | **PASS** | 核心循环（12 天 × 6 阶段）可收敛，结局可产出（(a) 已验证） |
| 平衡质量门 | **CONCERNS** | `lose=0`，玩家不会「输」，需数值调参后才算真正通过 |
| 总体放行建议 | **可发布 v0.1.0 供试玩** | 但需在后续 sprint 修复平衡偏差；建议用户/设计明确是否对平衡偏差予以豁免 |

> 说明：质量门为**建议性门控（advisory）**。最终放行由用户决定；High 级平衡偏差建议在发布说明中向试玩用户透明标注。

---

## 7. 修复「lose=0」的具体可落地建议【建议】

1. **提高 AI 攻击性（推荐优先）**
   - 在 `config/tuning.json` 中上调 NPC 在联席会议「指认」玩家的概率与权重，尤其在玩家声望领先时；使被投票出局（observer）与落败路径更可被触发。

2. **收紧胜负阈值**
   - 将胜利门槛由「声望 ≥ 80」上调至更高分位（如 ≥ 88），或要求「达成派系目标」与「声望 ≥ 80」同时满足；同时把压力 ≥ 100 的触发更敏感（如关键负面事件额外叠加压力）。

3. **增加负面事件权重 + 降低玩家初始资源**
   - 在 `cards_data.py` 选项权重与 `tuning.json` 中提升负面/高压选项出现率；下调玩家初始预算与缓冲资源，使随机策略下 `lose`/`observer` 出现非零分布。
   - 调参后用 headless（多 seed、多 difficulty）回归，目标：normal 下 `lose` 占比 > 0 且整体分布合理（建议胜率 40–60% 区间）。

---

*文档结束。质量门与修复建议均以「已验证事实 + 建议」双层标注，供设计/数值与发布决策参考。*
